const Words = () =>{
    const addWords = (userSentence)=>{
        // const addedWords = [];

        if(userSentence != ''){
            console.log(userSentence);
            // addedWords.push(userSentence);
            // console.log(addedWords);

            return userSentence;

        
        } 
    }

    return{
        addWords,
    }
}